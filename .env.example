# Clé API Google Gemini (obligatoire pour l'analyse réelle).
# Sur GitHub : Settings > Secrets and variables > Actions > New repository secret.
GEMINI_API_KEY=

# Modèle Gemini utilisé (par défaut : gemini-3.5-flash).
# Modifiable sans toucher au code.
GEMINI_MODEL=gemini-3.5-flash
